#CURRENT WORKING DIRECTORy
pwd

#make directory
mkdir directoryname

#To look in your current working directory
ls
#
ls -l

#if a file starts with d it means its a directory file(folder)
#if a file starts with - it means its a  file

# To create a new file command should be

touch linux.txt
#or
touch linux.yml

#remove a  directory we use 
rmdir mypc

#to change your current working directry

cd newWkdir

#to get back to previous directory or out of the folder 
cd ..

#If we try to remove a directory having some data it is not possible a directory has to be empty before deleted
# But to force dlt the directory command is 
rm -rf directoryName

#To display your all files and sort in them in latest time created 
ls -lt
#To reverse it 
ls -ltr
#or
ls -lr

#to delete all files with a certain extension example .txt then 
rm *.txt 


