# To get lines from a file where it contains a certain word like sample then use
grep "sample" sample.txt
#to ignore case sensitivity
grep  -i "sample" sample.txt
# to get lines where it do not contain a certian word lets sample then 
grep -v "sample" sample.txt
